import React from 'react';
import { IonPage, IonContent, IonGrid, IonRow, IonCol, IonText, IonRouterLink } from '@ionic/react';

const Dashboard: React.FC = () => {
    return (
        <IonPage>
            <div>Dashboard</div>
        </IonPage>
    );
};

export default Dashboard;